sets = {}
sets.pre_action = {}
sets.pre_action.FC = {
}

sets.idle = {
}

filter_action = function(act)
end

pre_action = function(act)
end

mid_action = function(act)
end

post_action = function(act)
end